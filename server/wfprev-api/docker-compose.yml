services:
#  api:
#    build:
#      context: .
#      # Use the updated Dockerfile.graalvm
#      dockerfile: Dockerfile.graalvm
#      # Pass the maven settings file as a build arg
#      args:
#        - MAVEN_SETTINGS_FILE=vivid.settings.xml
#    environment:
#      WFPREV_DATASOURCE_URL: ${WFPREV_DATASOURCE_URL}
#      WFPREV_DATASOURCE_USERNAME: ${WFPREV_DATASOURCE_USERNAME}
#      WFPREV_DATASOURCE_PASSWORD: ${WFPREV_DATASOURCE_PASSWORD}
#    ports:
#      - "8080:8080"
#      - "1336:1336"
#    depends_on:
#      db:
#        condition: service_healthy
#    networks:
#      - wfprev-network

  db:
    image: postgis/postgis:16-3.4
    container_name: wfprev-postgres
    environment:
      POSTGRES_USER: wfprev
      POSTGRES_PASSWORD: ${WFPREV_DATASOURCE_PASSWORD}
      POSTGRES_DB: wfprev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - wfprev-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wfprev -d wfprev"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

#  liquibase:
#    image: liquibase/liquibase
#    command: update
#    environment:
#      LIQUIBASE_URL: ${WFPREV_DATASOURCE_URL}
#      LIQUIBASE_USERNAME: ${WFPREV_DATASOURCE_USERNAME}
#      LIQUIBASE_PASSWORD: ${WFPREV_DATASOURCE_PASSWORD}
#      LIQUIBASE_CHANGELOG: classpath:/db/changelog/changelog-master.xml
#    volumes:
#      - ./src/main/resources/db/changelog:/liquibase/changelog
#    depends_on:
#      db:
#        condition: service_healthy
#    networks:
#      - wfprev-network

networks:
  wfprev-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local